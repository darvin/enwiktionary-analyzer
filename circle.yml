deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
      - npm run 2npm
test:
  post:
    - npm run test_long
    - istanbul cover node_modules/mocha/bin/_mocha
    - bash <(curl -s https://codecov.io/bash)

